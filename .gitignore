# Secrets -- never commit these.
credentials.json
token.json
.env

cache.json
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
src/*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# Phase 1: ingestion pipeline
data/

# Belt and braces. Books now arrive through the upload endpoint or `--local`
# and land in data/uploads/ (already ignored), so nothing should drop a PDF in
# the repo root any more -- but they are 20 MB+ and must never enter git
# history, so the rule stays.
*.pdf
